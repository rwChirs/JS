

// 字符串扩展：（第二部分 模板字符串）

// 单引和双引都是普通字符串
// let str = "my name is admins!";

// ES6 提供了新的定义字符串的方法
// 使用反引号 `

// 如：
// let str = `my name is admins!`;
// console.log(str);

// 在模板字符串可以 解析变量
let [name] = ['小明']; //使用数组结构
let str = `my name is ${name}`;
console.log(str);//my name is 小明
// 当成变量对待，若没有这个写法，还得用+做拼接字符串





































